language: python
python:
    - "2.7"

virtualenv:
    system_site_packages: true

before_install:
    - sudo apt-get install -qq python-numpy python-scipy

script:
    - python dictionary_spline_test.py
    - python get_times_test.py

notifications:
    email:
        - matyas.vegh@gmail.com
    on_success: change
    on_failure: always

